# gtb-chat-backend

-clone the repositor.
open terminal or command prompt in the folder directory                   .
enter this command: pip install -r requirements.txt                  .
after it finishes type this command: uvicorn main:app --reload           .
